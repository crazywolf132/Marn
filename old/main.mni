load "folders.mni";
load "managers.mni";
load "marn.mni";
load "output.mni";

let Fol = Folders();
let Out = Output();
let Man = Managers(Out);

let marn = Marn(Man, Fol, Out);

marn.install("chalk");